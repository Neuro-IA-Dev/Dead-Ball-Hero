# PROGRESS.md — DEAD BALL HERO

> Claude Code: lee CLAUDE.md y ROADMAP.md antes de tocar código. Implementa la siguiente tarea `[ ]` en orden, márcala `[x]` al terminar con fecha y nota breve. Un commit por tarea. Si una tarea revela trabajo extra, agrégala como subtarea, no lo hagas en silencio.

## Estado actual
**Fase:** 1 — MVP
**Última sesión:** 2026-06-11 — **1.9f Ejecución FC26 v1** (pedido del usuario: priorizar que la EJECUCIÓN del tiro se sienta como FC26, dé ganas de seguir tirando y enseñe la técnica real). Cinco frentes, sin tocar las constantes afinadas del solver: (1) **carrera real del pateador** — corre de la pose de espera (2.8 m atrás, diagonal al pie hábil) hacia el balón acelerando, con zancadas/brazos en oposición, planta y barre la pierna con el golpe sincronizado al contacto (RUNUP→FLIGHT); (2) **punch de impacto** — golpe de cámara (zoom-in que se abre, 200 ms) + pop de deformación del balón (130 ms); (3) **audio sintetizado rico** — silbato del árbitro al apuntar, golpe con cuerpo grave + pitch por potencia, whoosh del balón en vuelo (∝ velocidad), swish de red al gol, lamento de la multitud al fallar, palo metálico mejorado, explosión de gol con barrido de filtro; (4) **vuelo más vivo** — rodadura por velocidad + spin de receta, estela aditiva más brillante; (5) **capa de entrenamiento** (`game/coach.ts`) — consejo de técnica real por tipo de tiro mientras apuntas + diagnóstico post-tiro de una línea desde datos reales del solver (evento, Δpotencia, altura máx). Textos en `es.json` (`coach.*`). 35 tests verdes (7 nuevos del coach); typecheck/lint/build OK.
**Revisión del usuario (2026-06-11):** aprobó avanzar tras probar la barrera; reportó freeze en rasos/ángulos, corregido parcialmente. Pendiente: prueba en vivo de la nueva ejecución (carrera + audio + impacto).
**Sesión 2026-06-11 (cont. 2):** **dificultad + gráficos**. (a) **Arquero reforzado**: más alcance lateral (`maxReachX` 2.05→2.75), plantado más ancho (0.85), reacción 0.3 s, dive 5.6 m/s — ahora ataja el centro y obliga a buscar los ángulos. (b) **Barrera que salta**: `jumpChance` por nivel (a1n04 0.4, a1n05 0.5) + `?wallJump=` en sandbox; al saltar deja hueco abajo (el raso pasa) y sube el techo (el globo por arriba se complica). Colisión con `riseY` + animación del actor. (c) **Gráficos**: post-procesado **bloom** (EffectComposer), estadio nocturno (`stadium.ts`) con **multitud instanciada** (~2900 espectadores de colores), torres de luz que florecen, **pantalla con el logo**, vallas; cielo en gradiente, **líneas del campo** (área, área chica, arco/punto penal), red más densa. 64 tests; typecheck/lint/build OK.
**Sesión 2026-06-11 (cont.):** **fix freeze + loop de juego MVP**. (a) Arreglado el freeze de rasos/costados (rodadura vs bote en `collisions.ts` + detección de balón muerto) y el diagnóstico falso de "a las nubes". (b) Dialecto a **español de Chile**. (c) **Loop de juego (1.13–1.16)**: loader de niveles JSON con validación, sesión + evaluación de estrellas, almacén de progreso en localStorage, HUD de estado + panel de estrellas/reintentar/siguiente, y menú de selección de nivel con candados y estrellas. 62 tests verdes; typecheck/lint/build OK.
**Bloqueos:** ninguno.
**Siguiente:** validación en vivo del usuario (ejecución 1.9f + loop de niveles). Luego 1.12 (cámara de vuelo/replay más cinematográfico), roster real (1.14 completo) y 1.18 (QA de feeling con receta canónica).

---

## FASE 1 — MVP

### Setup
- [x] 1.1 Inicializar proyecto Vite + TypeScript + Three.js. Estructura de carpetas según CLAUDE.md. ESLint básico. `npm run dev` funcionando. _(2026-06-11: Vite 8 + TS 6 estricto + three r184. Carpetas src/{core,game,render,ui,data/{levels,locales}}. Scripts: dev/build/preview/typecheck/lint. ESLint flat config. Alias `@`→src. main.ts es un placeholder (cubo girando) que se reemplaza en 1.2. typecheck+lint+build OK, `npm run dev`→HTTP 200.)_
- [x] 1.1b Sistema i18n mínimo: helper `t(clave)` + `src/data/locales/es.json` con TODOS los textos del juego desde el primer día (español por defecto, estructura lista para `en.json`). Prohibido hardcodear strings en componentes. _(2026-06-11: `core/i18n.ts` con `t(key, params)` (claves con punto + interpolación `{var}`), `detectLocale()` por navegador, fallback a es, clave cruda + warn si falta (regla LEVELS.md). `es.json` cubre app/menu/settings/hud/result/shot/stars/corner/weather/acts/kickers + los 5 niveles del Acto 1. `main.ts` fija el título vía `t('app.title')`. Estructura lista para `en.json` (Fase 4, idealmente import dinámico).)_
- [x] 1.2 Escena base: campo (plano con textura procedural de césped), arco reglamentario 7.32×2.44 m con red (líneas), iluminación tipo estadio nocturno, cielo oscuro. Cámara detrás del punto de tiro. _(2026-06-11: `core/field.ts` (dims+coords compartidas), `render/renderer.ts` (renderer+cámara+resize, sombras PCF, ACES), `render/world.ts` (césped procedural por CanvasTexture con franjas, arco cilíndrico + red de líneas, 4 torres SpotLight, fog nocturna), `render/ball.ts` (icosaedro flat-shaded). Cámara broadcast detrás del balón.)_

### Física
- [x] 1.3 Módulo `core/ballistics.ts`: integrador del balón con gravedad, drag y Magnus según parámetros de CLAUDE.md. Test unitario: con 3 barras y curva máxima, desplazamiento lateral ≈3 m a 25 m. _(2026-06-11: `core/physics.ts` (constantes + `speedForPower`), `core/ballistics.ts` (`stepBall` Euler semi-implícito con scratch vectors + `traceTrajectory` para test y línea de apuntado). Vitest instalado, test verde. **MAGNUS_S calibrado a 0.00169** → comba ≈3 m a 25 m con 3 barras y `MAX_CURVE_SPIN=60`.)_
- [x] 1.4 Colisiones: postes/travesaño (rebote), red (gol + frenado), suelo (rebote amortiguado), fuera (out). Eventos: `GOAL`, `POST`, `SAVED`, `WALL`, `OUT`. _(2026-06-11: `core/collisions.ts`: `resolveGoalLine` (interpola el cruce en z=0 → GOAL/POST/CROSSBAR/OUT), `bounceGround` (restitución 0.45, fricción 0.72, amortigua spin), `isOutOfPlay`, y `ShotCollider` con estado (recuerda pos previa). Enum incluye SAVED/WALL para 1.10/1.11. `collisions.test.ts`: 7 casos verdes.)_

### Input y mecánica de tiro
- [x] 1.5 Máquina de estados del tiro: `AIMING → CONTACT_SELECT → POWERING → TIMING → FLIGHT → RESULT`. _(2026-06-11: `game/shot-machine.ts`. ⚠️ Se modifica en 1.9b.1: desaparece la fase TIMING.)_
- [x] 1.6 Apuntado: retícula movible (mouse/táctil/teclado), línea de trayectoria parcial cuya longitud viene del stat LÍNEA del pateador. _(2026-06-11: `render/aim.ts`, `game/kicker.ts`, `game/flight.ts`, `game/game.ts`, `ui/hud.ts`. ⚠️ Se modifica en 1.9b.2: el apuntado pasa a azimut + línea de proyección, sin retícula en el arco.)_
- [x] 1.7 Grilla de contacto simplificada MVP. _(2026-06-11: `ContactPad` con grilla 3×3 + punto rojo. ⚠️ Se reemplaza en 1.9b.3 por el balón estilo consola con puntero y etiqueta.)_
- [x] 1.8 Barra de potencia de 5 segmentos + ventana de timing verde. _(2026-06-11: `PowerBar` + `TimingBar` + `core/audio.ts`. ⚠️ La `TimingBar` se ELIMINA en 1.9b.1; la `PowerBar` se conserva y gana el feedback de "potencia perfecta" en 1.9b.4.)_
- [x] 1.9 Mapeo del resultado: (apuntado, contacto, potencia, timing) → velocidad inicial + spin. _(2026-06-11: `game/shot-solver.ts` con apuntado balístico por bisección, `contactToSpin`, `dispersionSigma`, ruido gaussiano. ⚠️ Se adapta en 1.9b: el timing sale de la fórmula de dispersión; el apuntado balístico a punto se reemplaza por elevación derivada del contacto+potencia.)_

### ⚑ 1.9b — REWORK UX: paridad con la edición 26 del referente (PRIORIDAD MÁXIMA, antes de 1.10)
> Origen: prueba en vivo del usuario + verificación contra el referente real. La edición 26 NO tiene timing verde (doble toque): es UNA barra de mantener-soltar. Y la selección de contacto es un balón con puntero fino y etiqueta de tipo de golpe, no una grilla con punto rojo. La especificación completa quedó en CLAUDE.md (sección "La mecánica core") — releerla antes de empezar.

- [ ] 1.9b.1 **Una sola barra, sin doble toque.** Eliminar `TimingBar` y la fase `TIMING` de `shot-machine.ts` (nuevo flujo: `AIMING → CONTACT_SELECT → POWERING → RUNUP → FLIGHT → RESULT`). Al SOLTAR el botón/click/touch: potencia fijada + arranca `RUNUP` (carrera del pateador, 600 ms para Diego) + golpeo automático al terminar. Durante POWERING la barra se llena en 1150 ms; si llega al tope sin soltar, dispara al máximo (igual que el referente). Actualizar tests de la máquina.
- [ ] 1.9b.2 **Apuntado por azimut con línea de proyección (sin retícula en el arco).** El arrastre horizontal (o ←/→) rota la dirección del tiro alrededor del balón y la cámara orbita acompañando. La elevación YA NO la elige el usuario apuntando a un punto: se deriva de `contacto.Y` + tipo de tiro + potencia (tabla de elevación base por tiro en `shot-solver.ts`). La línea de proyección sale DESDE EL BALÓN, muestra el primer tramo de la trayectoria real (con comba y elevación actuales, recalculada en vivo) y su largo = stat LÍNEA. Eliminar la retícula del arco. El gesto de juego correcto pasa a ser "alinear la línea sobre/al costado de la barrera", como en el referente. **Encuadre de cámara según `docs/ref-contacto-fc26.png`:** cámara baja detrás y levemente al costado del pateador (lado de su pie hábil), pateador en el tercio izquierdo del cuadro, balón en el tercio inferior-central, arco centrado al fondo. Línea de proyección en verde.
- [ ] 1.9b.3 **Selector de contacto EN EL MUNDO 3D, calcado de la edición 26.** El usuario aportó captura de referencia: guardarla en `docs/ref-contacto-fc26.png` y replicar su composición (solo como referencia interna, jamás incluirla en el juego ni en assets). Eliminar el `ContactPad` del HUD. Implementar en Three.js, anclado al balón: (a) **grilla romboidal verde semitransparente** — plano billboard inclinado ~45° detrás/alrededor del balón, líneas finas 5×5, opacidad ~0.35; (b) **punto rojo** (sprite) que marca el contacto y se mueve con arrastre táctil/mouse o WASD/stick derecho, limitado al rombo; (c) **cruz/cursor pequeña** junto al punto; (d) **etiqueta inferior en mayúsculas** (franja oscura semitransparente, tipografía condensada) con el tipo de golpe según zona. Claves nuevas en `es.json`: `shot.chanfle_interior` ("CHANFLE CON EL INTERIOR DEL PIE"), `shot.chanfle_exterior` ("CHANFLE CON EL EXTERIOR DEL PIE"), `shot.picada` ("PICADA"), `shot.raso` ("RASO"), `shot.normal` ("GOLPE NATURAL"). Zonas: |x|>0.45 → chanfle interior/exterior según signo y pie del pateador; y>0.45 → picada; y<-0.45 → raso; centro → natural. La grilla y el punto solo son visibles en fases AIMING/CONTACT_SELECT/POWERING; desaparecen al disparar. La PowerBar sigue siendo elemento aparte del HUD (abajo), sin tocar.
- [ ] 1.9b.4 **"Potencia perfecta" reemplaza al verde.** En `shot-solver.ts`: `dispersionSigma` ya no recibe timing; recibe `|power - centroÓptimoDelTiro|`. Soltar dentro de ±0.15 barras del centro → dispersión 0 + la PowerBar emite un brillo/flash dorado + `playPerfect()` en audio + `navigator.vibrate(40)` si existe. El rango óptimo por tipo de tiro vive en una tabla (curva: centro 2.75; raso: 2.0; etc., según recetas de CLAUDE.md). Actualizar `shot-solver.test.ts`.
- [ ] 1.9b.5 **Stars y locale.** `StarCondition` cambia `green_timing` → `perfect_power` (LEVELS.md ya actualizado, copiar). Actualizar los 5 JSON de niveles y el texto del nivel a1n03 ("La potencia justa") desde LEVELS.md. Migración de saves: si localStorage tiene estrellas con la condición vieja, conservarlas tal cual (no penalizar al jugador).
- [ ] 1.9b.6 **QA del rework con overlay de debug.** Toggle `?debug=1`: muestra azimut, contacto, potencia, sigma y punto de cruce en z=0. Verificar la receta canónica: línea al costado de una barrera imaginaria + contacto lateral + soltar en 2.75 → gol consistente. Anotar parámetros en Notas de QA. **⚑ avisar al usuario para probar en vivo antes de seguir con 1.10.**

### ⚑⚑ 1.9c — ARREGLO DE SENSACIÓN DE TIRO (FIX URGENTE — la validación del usuario FALLÓ)
> Veredicto del usuario en vivo (2026-06-11): "Al mover se mueve la cancha, no la mira. No es posible apuntar. La mira del balón está descuadrada de donde pegarle. No dan ganas de tirar." Diagnóstico confirmado contra el referente: la edición 26 SÍ tiene una retícula circular de caída que es LO QUE SE MUEVE con el stick; la cámara queda casi fija. Implementamos rotación de cámara/azimut: incorrecto. CLAUDE.md ya está corregido — releer la sección "La mecánica core" completa antes de empezar. No avanzar a 1.10 hasta que el usuario apruebe 1.9c.5.

- [x] 1.9c.1 **El stick mueve LA MIRA, no el mundo.** Implementar retícula circular (anillo verde, ~0.5 m de radio visual) que vive en el plano del arco (z=0) y se mueve con arrastre/flechas en X (libre entre los palos ±2 m de margen) e Y (0.2–2.6 m). La línea de proyección punteada conecta balón→retícula usando la trayectoria balística real (recuperar la bisección de 1.9: `solveLaunchDirection` VUELVE a ser el modelo — la decisión de 1.9b.2 de derivar elevación queda revertida; el contacto.Y ahora modula caída/comba sobre la trayectoria a la retícula, no la elevación base). Recortar la línea al stat LÍNEA. Sensibilidad: cruzar el ancho del arco toma ~1.2 s a velocidad máxima; respuesta cuadrática para permitir ajustes finos de centímetros. _(2026-06-11, commit 25ff030: aim `{azimuth}`→`{x,y}`; `solveLaunchDirection` restaurada; `aim.ts` retícula circular verde (radio∝dispersión) + línea ball→retícula; puntero raycast al plano z=0 (arregla bug móvil); flechas con rampa cuadrática.)_
- [x] 1.9c.2 **Cámara casi fija con seguimiento amortiguado.** La cámara base queda detrás del balón mirando al centro del arco. Su yaw/pitch siguen a la retícula con factor 0.2 y suavizado exponencial (tau ≈ 0.25 s), con tope duro de ±10° de yaw y ±4° de pitch. PROHIBIDO que el escenario gire 1:1 con el input. Criterio: mover la mira de palo a palo desplaza el horizonte de forma apenas perceptible; el arco JAMÁS sale del cuadro. _(2026-06-11, commit c5fd88f: `updateAimCamera` posición fija; yaw/pitch siguen la retícula con factor 0.2 + suavizado `α=1−e^(−dt/0.25)` + topes ±10°/±4°; giro real ~1–2°.)_
- [x] 1.9c.3 **Recuadrar el selector de contacto al balón.** La grilla romboidal + punto rojo + cruz deben estar ancladas al CENTRO del balón: grupo Three.js hijo de la posición del balón, billboard hacia cámara, escala ~2.5× el radio del balón. El punto rojo se mueve en coordenadas locales del rombo. Verificar con `?debug=1` (dibujar la proyección 2D del centro del balón y comprobar coincidencia visual a 3 distancias de cámara). Error máximo tolerado: 5% del radio del balón en pantalla. _(2026-06-11, commit 2cdd336: `ContactSelector` re-anclado a `ball.getWorldPosition` cada frame (origen=centro del balón ⇒ contacto (0,0)=centro), escala 2.5×radio, billboard+tilt 36°, `depthTest:false`. `?debug=1` → `selErr` ~0px, ≪5%.)_
- [x] 1.9c.4 **Juice del disparo — que den ganas de tirar otra vez.** (a) Micro hit-stop de 70 ms al golpear + sonido de golpe con pitch ligado a potencia; (b) trail sutil del balón en vuelo (línea que se desvanece, 0.3 s); (c) al GOL: ondulación de la red (desplazamiento simple de vértices o sprite animado), rugido de multitud, leve shake de cámara (4 px, 200 ms), texto "¡GOOOL!" con escala animada; (d) al POSTE: "clank" metálico seco + zoom corto; (e) reset al siguiente intento en <1.5 s, saltable con un toque; (f) repetición de gol de 2 s desde detrás del arco, saltable. Nada de esperas: el ciclo tirar→ver→volver a tirar debe sentirse inmediato. _(2026-06-11: (a) hit-stop 70 ms + `playKick(power)` pitch∝potencia; (b) `BallTrail` (estela ≤0.3 s, fade por color); (c) GOL: `NetRipple` (vértices del fondo de la red), `playCrowd`, shake 0.05 m·200 ms, "¡GOOOL!" animado; (d) POSTE: `playPost` + zoom FOV −6° 220 ms; (e) auto-reset 1.2 s saltable con toque; (f) repetición de 2 s desde detrás del arco (cámara `replayCamPos`, reproduce `samples`), saltable. `world.ts` expone la red. typecheck+lint+build OK, 22 tests.)_
- [x] 1.9c.5 **Validación medible + sesión con el usuario.** Checklist de aceptación antes de avisar: (1) mover el stick nunca rota la cámara más de 10°; (2) la retícula y la línea responden sin lag perceptible (<50 ms); (3) selector de contacto perfectamente centrado en el balón a toda distancia; (4) receta canónica (mira al costado de la barrera imaginaria + chanfle + soltar en 2.75) = gol en ≥7 de 10 intentos con potencia perfecta; (5) ciclo entre intentos <2 s; (6) jugar 10 tiros seguidos y preguntarse honestamente "¿quiero tirar el 11º?". Documentar parámetros en Notas de QA y ⚑ avisar al usuario. _(2026-06-11: **parte medible verificada** (1–5 ✓, ver Notas de QA; receta 10/10), documentada. Usuario autorizó avanzar; se desbloqueó 1.10.)_

### ⚑ 1.9d — REFACTOR DE FEELING / SOLVER COMPARTIDO (pedido explícito del usuario, 2026-06-11)
- [x] 1.9d.1 **Separar `ShotIntent` y `ShotLaunch`.** Crear contrato explícito entre input de entrenamiento y lanzamiento físico (`src/game/shot-model.ts`), con debug auditables (azimut, contacto, potencia, elevación, spin, Cd, Magnus, cruce, resultado estimado). _(2026-06-11: `ShotIntent`, `ShotLaunch` y `ShotDebugInfo` introducidos; `shot-solver.ts` dejó de mezclar todo en `BallState` crudo.)_
- [x] 1.9d.2 **Unificar solver, recetas y física fija.** `shot-solver.ts` ahora resuelve desde recetas centrales (`SHOT_RECIPES`) y `core/ballistics.ts` comparte `FIXED_TIMESTEP=1/120`, `dragCd` y `magnusScale` entre preview y tiro real. _(2026-06-11: `Flight` usa el mismo timestep exportado; cada tiro sale con su `dragCd`/`magnusScale` reales; preview y vuelo usan el mismo launch.)_
- [x] 1.9d.3 **`TrajectoryPreview` con el mismo pipeline del tiro real.** Reemplazar la línea de ayuda por una preview que simula desde `ShotIntent → ShotLaunch → BallState → traceTrajectory` usando el mismo solver/física, con glow básico y fade. _(2026-06-11: nuevo `render/trajectory-preview.ts`; `AimVisuals` compone retícula + preview y ya no calcula una línea "aparte".)_
- [x] 1.9d.4 **Cámara y feedback de entrenamiento sin rehacer la UI.** Extraer `FreeKickCameraRig`, sumar overlay fantasma de inputs y panel corto de análisis post-tiro, manteniendo el HUD grande casi intacto. _(2026-06-11: `render/free-kick-camera.ts`, overlay `ghost-overlay`, panel `analysis-panel`; sin menús nuevos ni cambios grandes de layout.)_
- [x] 1.9d.5 **Expandir `?debug=1`, QA y validación técnica.** `?debug=1` ahora muestra azimut, contacto, tipo, potencia, velocidad inicial, elevación, spin, Cd, Magnus, sigma, altura en barrera, cruce en arco y resultado estimado. `typecheck`, `lint`, `test` y `build` ejecutados OK. _(2026-06-11: 23 tests verdes; build OK.)_

### ⚑⚑⚑ 1.9e — RESET DEL CORE DEL TIRO (prioridad absoluta, 2026-06-11)
- [x] 1.9e.1 **Corregir la dirección del disparo.** Abandonar la elevación resuelta hacia una retícula alta y separar explícitamente azimut horizontal y elevación por receta. Crear `directionFromAzimuthAndElevation(baseDirectionToGoal, aimAzimuthDeg, elevationDeg)`. _(2026-06-11: `shot-solver.ts` ahora compone dirección a partir de base hacia el arco + azimut en XZ + elevación en Y; desaparece la posibilidad de un vector casi vertical por `contactY`.)_
- [x] 1.9e.2 **Centralizar recetas y clamps de ángulo.** `SHOT_RECIPES` ahora define `launchAngleDeg`, `minAngleDeg`, `maxAngleDeg`, `idealPower`, `dragCd`, `sideSpin`, `topSpin` por tipo. _(2026-06-11: inside curve 17° clamp 15–19; natural 14° clamp 10–18; raso 3.5° clamp 2–5; knuckle 14° clamp 11–17.)_
- [x] 1.9e.3 **Mantener potencia realista y curva legible.** `speedForPower()` se conserva lineal 18→38 m/s; el tiro canónico de 2.85 barras sale a ~27.25 m/s. Spin orientado como `Vector3(topSpin, sideSpin, 0)`. _(2026-06-11: tiro canónico validado en tests dentro del rango 27–30 m/s; `MAGNUS_S` se mantiene editable.)_
- [x] 1.9e.4 **Preview = tiro real + fixed timestep.** `TrajectoryPreview` sigue usando exactamente el mismo `ShotIntent`, `ShotSolver`, `dragCd`, `magnusScale` y `FIXED_TIMESTEP=1/120` que el vuelo real. _(2026-06-11: tests de consistencia siguen verdes; no se volvió a introducir línea decorativa separada.)_
- [x] 1.9e.5 **Cámara jugable mínima + modo canónico temporal.** Ajustar la cámara para leer balón/arco/trayectoria y añadir `?canonical=1` con el tiro interior de 25 m (pie derecho, `contactX=0.65`, `contactY=0.05`, `power=2.85`, `azimut=-6°`). _(2026-06-11: `TRAINING_RIGHT_FOOT`, ball a 25 m, seed automático de mira/contacto/potencia, FOV 50, cámara detrás del balón más legible.)_
- [x] 1.9e.6 **Tests mínimos del core.** Añadir tests para curva interior 25 m, raso 18 m y natural 22 m. _(2026-06-11: `shot-solver.test.ts` ahora valida velocidad, ángulos, cruce y ausencia de trayectorias casi verticales.)_

### ⚑ 1.9f — EJECUCIÓN FC26 v1 (pedido del usuario: la ejecución del tiro como prioridad)
> Meta: que rematar se sienta como FC26, dé ganas de tirar otra vez y enseñe la técnica real "de paso". No se tocan las constantes del solver (afinadas y probadas).
- [x] 1.9f.1 **Carrera real del pateador.** `KickerActor` corre de la pose de espera hacia el balón acelerando, con zancadas y brazos en oposición, planta y barre la pierna con el golpe sincronizado al contacto. _(2026-06-11: `src/render/actors.ts`; standPos/plantPos por pie hábil; swing peak en `p=1`=FLIGHT.)_
- [x] 1.9f.2 **Punch de impacto.** Golpe de cámara (zoom-in 200 ms que se abre) + pop de deformación del balón (130 ms). _(2026-06-11: `applyStrikePunch`/`updateBallSquash` en `game.ts`.)_
- [x] 1.9f.3 **Audio sintetizado rico.** Silbato, golpe con cuerpo + pitch por potencia, whoosh ∝ velocidad, red, lamento, palo, explosión de gol. _(2026-06-11: `src/core/audio.ts` reescrito con master gain.)_
- [x] 1.9f.4 **Vuelo más vivo.** Rodadura por velocidad + spin de receta; estela aditiva más brillante. _(2026-06-11: `spinBall` en `game.ts`, `ball-trail.ts`.)_
- [x] 1.9f.5 **Capa de entrenamiento.** Consejo de técnica por tipo de tiro en vivo + diagnóstico post-tiro determinista. _(2026-06-11: `src/game/coach.ts`, `coach.*` en `es.json`, `coach.test.ts`, línea de coach en HUD/CSS.)_
- [ ] 1.9f.6 **⚑ Validación en vivo con el usuario.** Probar carrera/audio/impacto en `npm run dev`; ajustar ritmo de carrera, volúmenes y si el silbato por intento cansa. Documentar y avisar.

### Mundo
- [x] 1.10 Barrera estática: N cápsulas según JSON del nivel, colisión = `WALL`. _(2026-06-11: `BarrierSetup`/`BarrierColliderConfig` en `core/collisions.ts`; `ShotCollider` detecta `WALL`; `Flight`, `ShotSolver` y `TrajectoryPreview` reciben la misma barrera; `render/barrier.ts` muestra jugadores low-poly. Prueba temporal hasta 1.13: `?wall=4` o `?wall=5`, opcional `wallDistance=9.15`.)_
- [x] 1.11 Arquero básico: reacción tras un delay paramétrico, se lanza hacia la intersección estimada; si llega, `SAVED` con animación simple. _(2026-06-11: `game/goalkeeper.ts` separa planificación y estado (`waiting`→`diving`→`saved/missed`); `Game` lo conecta al vuelo real; `GoalkeeperActor` ya no anticipa la mira antes del tiro y resetea brazos. URL temporal: `keeper=0`, `keeperDelay=<0.12..0.9>`, `keeperSpeed=<1.6..6.5>`.)_
- [ ] 1.12 Cámara de vuelo que sigue el balón + repetición corta del gol desde detrás del arco.

### Contenido y meta
- [x] 1.13 Formato JSON de nivel + loader con validación, implementando EXACTAMENTE el esquema de **LEVELS.md**. _(2026-06-11: `src/game/level.ts` (tipos `LevelSpec`/`StarCondition` + `validateLevel`/`parseLevels` con campo culpable en el error), `src/data/levels/act1.json` (los 5 niveles del Acto 1). Tests en `level.test.ts`. Niveles referenciados por `id`, nunca índice.)_
- [x] 1.14 Pateador Diego con stats (PRE 95, POT 80, CUR 92, KNU 60, LÍNEA larga). _(2026-06-11: Diego ya tenía stats que afectan dispersión (`shot-solver`) y línea (`aidLine`); `loadLevel` aplica `forcedKicker`→Diego y `aidLineOverride`. Roster completo y selección de pateador = Fase 2/3.)_
- [x] 1.15 HUD completo: intentos, contexto del nivel, resultado, estrellas, botones reintentar/siguiente. _(2026-06-11: `Hud.setStatus` (barra superior nombre/goles/intentos/minuto-marcador) + `LevelPanel` (estrellas 1–3 + Reintentar/Siguiente/Menú, único elemento interactivo). `level-session.ts` evalúa estrellas; tests en `level-session.test.ts`.)_
- [x] 1.16 Menú mínimo: título, selección de nivel (candados y estrellas), persistencia en localStorage. _(2026-06-11: `src/game/app.ts` (App shell, menú con grilla de niveles, candados en cadena, estrellas, monedas), `src/game/progress-store.ts` (localStorage tolerante a corrupción, sube estrellas sin bajar, monedas al primer clear). Tests en `progress-store.test.ts`. `main.ts` arranca en el menú; `?canonical/?wall/?keeper` saltan al sandbox de QA.)_
- [x] 1.17 Audio mínimo: golpe al balón, palo, red, murmullo/explosión de multitud. _(2026-06-11: cubierto en 1.9f — `src/core/audio.ts` sintetizado: silbato, golpe con cuerpo, whoosh, red, palo, multitud, lamento.)_
- [ ] 1.18 QA de feeling: calibrar hasta que la receta canónica de curva (CLAUDE.md) dé gol consistente con potencia perfecta y falle de forma justa al pasarse o quedarse corto. Documentar parámetros finales aquí. **⚑ REVISIÓN CON USUARIO** ("se siente como la consola").
- [ ] 1.19 Build de producción + deploy estático (Vercel/Netlify/GitHub Pages). Probar en un teléfono real.

## Backlog inmediato (Fase 2 — no empezar sin cerrar Fase 1)
- [ ] **Informe del Entrenador v1** (prioridad 1 de Fase 2): diagnóstico post-tiro desde datos del solver (delta potencia vs óptimo, mira, zona de contacto, dispersión), máx. 2 correcciones, textos por mentor en es.json. Spec en CLAUDE.md.
- [ ] **Mando Fantasma v1** (prioridad 2): overlay de gamepad genérico, modo espejo (inputs en vivo) + modo fantasma (tus inputs vs ejecución ideal, post-tiro). Silueta propia, sin símbolos de Sony; etiquetas L1/L2/R1/R2 con glifos propios. Spec en CLAUDE.md.
- [ ] **Sistema de puntaje** con récord por nivel, bonus (potencia perfecta, diana, distancia, sin línea) y umbrales de estrellas por puntos; HUD según `docs/ref-vision-dbh.png`.
- [ ] Upgrade visual hacia `docs/ref-vision-dbh.png`: multitud, pantalla con logo, banderas, HUD de paneles oscuros, indicador de viento, panel de puntaje objetivo.
- [ ] Gamepad API (mando físico en PC; el Mando Fantasma espejo refleja el mando real).
- [ ] Sticks virtuales táctiles con zona muerta configurable + vibración.
- [ ] Contacto completo: knuckleball (gesto vertical + flutter), trivela, caída, debajo de barrera.
- [ ] Spin de refuerzo durante la carrera (gesto del stick derecho en RUNUP, ~+20% comba).
- [ ] Barrera que salta + arquero por niveles.

## Decisiones tomadas
- 2026-06-10: Motor Three.js web (no Frostbite/Unity/Godot) por peso y multiplataforma instantánea.
- 2026-06-10: Niveles 100% data-driven en JSON.
- 2026-06-10: Nombres de pila únicamente; cero marcas registradas.
- 2026-06-11: Nombre definitivo del juego: **DEAD BALL HERO**.
- 2026-06-11: Español como idioma base; i18n desde el día 1, inglés en Fase 4 (lanzamiento mundial).
- 2026-06-11: Momentos Legendarios: cada mentor cierra su capítulo recreando su tiro libre histórico (datos fieles, ambientación genérica, firma visual por gesto y colores, jamás nombres reales).
- 2026-06-11: **El referente es la edición 26 (PS5), no las antiguas.** Sin timing verde: una sola barra mantener-soltar; "potencia perfecta" como skill central; retícula circular de caída + cámara casi fija; contacto con grilla romboidal sobre el balón.
- 2026-06-11: **El juego es chileno, no argentino.** Todos los textos del juego usan español de Chile (tú, no voseo). Léxico chileno permitido ("cabro", "achuntar", "bacán"); evitar marcas argentinas ("pibe", "soltá/pegá/apuntá"). Aplica a coach, briefs y todo `es.json` futuro.
- 2026-06-11: **Identidad de producto: entrenador-juego.** Misión doble (disfrutar el gol + mejorar en la consola). Features insignia: Mando Fantasma (espejo/fantasma/tutor) e Informe del Entrenador. Referencia visual maestra: `docs/ref-vision-dbh.png`. Versus entre jugadores reales como Fase 5 (local → asíncrono por código → online).

## Notas de QA / feeling
*(Claude Code: anota aquí cada ajuste de física o timing con su razón.)*
- 2026-06-12 (fixes de mando/HUD por feedback): (1) **El stick-D ya no cuenta como DISPARO**: el `pointerdown`/`pointerup` global de `window` ignoraba el `stopPropagation` del stick; ahora `Game.isPadEvent(e)` (vía `pad.root.contains(e.target)`) descarta cualquier evento originado en el mando, así sticks y botón son independientes. (2) **El tipo de disparo no queda tapado por el mando**: `.contact-label` subió a `bottom:184px` (arriba del mando). (3) **Instrucciones eliminadas** (a pedido): `.hud-hint` con `display:none`. Descartados los chips de configuración del nivel (iban en contra de reducir estorbo). 79 tests; typecheck/lint/build OK.
- 2026-06-12 (HUD hacia la referencia `ref-vision-dbh1.png`): panel **DESAFÍO** (arriba-izq, `Hud.setObjectives`) con las 3 estrellas del nivel y tilde al cumplirse — `SessionStatus` ahora expone `oneMet/twoMet/threeMet`; `starConditionLabel(cond)` en `level.ts` da el texto i18n (potencia perfecta / esquina / sin línea / tiro X / todos los intentos). **Distancia al arco** (arriba-centro, `setDistance(level.ball.z)`). **Viento** (`setWind`, solo niveles con `level.wind`). **Nota del tiro** A+/B/C/D (`shotGrade` en `coach.ts`, badge derecha bajo el marcador, color por letra; se limpia al apuntar). Reubicada la línea del coach a `top:98px` para no chocar con la distancia. Claves nuevas en `hud.*` (challenge/distanceLabel/meters/windLabel/windValue). 79 tests; typecheck/lint/build OK. Pendiente de la referencia (v2 de HUD): leyenda de mando arriba-der, panel "Análisis del último tiro" extendido, chips de modo abajo-izq.
- 2026-06-12 (Mando Fantasma v1 — jugable + espejo): feature insignia, decisión "decidelo tú" → control táctil + espejo unificados. `src/ui/gamepad.ts` (`PhantomPad`): silueta genérica/legal (etiquetas STICK-I/STICK-D/DISPARO, sin símbolos de fabricante), 2 sticks con captura de puntero (`setPointerCapture` + `stopPropagation` para no disparar el `pointerdown` global) y botón DISPARO. **Control**: stick-I empuja la mira (`applyPadAim`, reusa `MAX_AIM_SPEED`), stick-D mueve el contacto (`applyPadContact`, `CONTACT_PAD_SPEED=1.4`, `PAD_DEADZONE=0.06`), DISPARO = press/release (mismo `press()/release()` del teclado). **Espejo**: `updatePadMirror` cada frame refleja mira/contacto/potencia y resalta el stick activo según la fase, aunque juegues con teclado/mouse. Visible al jugar (loadLevel/enableSandbox), oculto en menú (hideLevelUi). Flujo: stick-I apunta → tap DISPARO (→CONTACT) → stick-D contacto → mantener/soltar DISPARO (potencia→tiro). Pendiente: validación táctil en celular real; modos fantasma (tu tiro vs ideal) y tutor = v2.
- 2026-06-12 (mira fuera + roster + palo-bonus): **Retícula de caída ELIMINADA** (decisión del usuario: no existe en el referente y volvía trivial el tiro en PC). `src/render/aim.ts` quedó solo con la línea de proyección (escala con LÍNEA/ayuda del nivel). El **preview/predicción NO rebotan en el palo** (`bouncePosts` flag en `ShotCollider`/`traceTrajectory`; solo el vuelo real carambolea) — antes el preview revelaba el carom y arruinaba la escena. **Roster jugable** en `src/game/kicker.ts` (`ROSTER`: Diego, David, Andrea, Dinho, Leo, Juni, Cris, Roberto, Sini) con stats/`style` por especialidad; `Q` cicla por todo el plantel (`switchKicker`), espejando el contacto al cambiar de pie. Perfiles visuales por id en `actors.ts` (camiseta, número, peinado, barba). Peinados: `makeHairstyle` (short/buzz/curly/afro/headband/ponytail/bald) + `makeBeard`. **Palo y adentro** suma `POST_IN_BONUS=600` al puntaje (`score.ts`, vía `Flight.hitPost`→`ShotOutcome.hitPost`). **PENDIENTE/abierto:** sin la mira, móvil quedó más difícil ⇒ evaluar Mando Fantasma táctil (controles en pantalla).
- 2026-06-12 (formas de pegarle + arquero que vuela + puntaje + día/noche): tres mejoras sobre el trabajo visual manual del usuario, sin rehacerlo.
  **Formas de pegarle**: `Kicker.style: 'finesse'|'power'|'knuckle'|'natural'` (`src/game/kicker.ts`; Diego=finesse, training-right=power). `KICK_STYLES` en `src/render/actors.ts` modula carrera (approach/sideStand/strides), plantado, cargada/follow-through (windupPeak/swingPeak) y giro del pie (footTurn = interior vs empeine). 'natural' conserva los valores previos.
  **Arquero que vuela**: `GoalkeeperActor.diveTo` ahora despega del suelo (`targetY` ∝ alcance/altura), gira el torso a horizontal (`targetLean`) y estira brazos; `update` interpola x/y/lean con tau 0.12 (clavada explosiva). Antes solo deslizaba en X.
  **Puntaje**: `src/game/score.ts` (`computeShotScore`: base 1000 + perfecta 500 + sin línea 300 + distancia (>16 m)·30 + ángulo hasta 500 por cercanía a la esquina). `LevelSession` acumula `score`; `progress-store` guarda `scores` (récord por nivel, `bestScoreFor`). HUD muestra puntaje en estado y panel. Tests en `score.test.ts`. **Estrellas + puntaje conviven** (progresión vs competencia).
  **Día/noche**: `World.setTimeOfDay('clear'|'rain'|'night')` en `src/render/world.ts` cambia cielo (3 gradientes), niebla, hemisférica, sol/luna y focos. `Game.loadLevel` lo aplica desde `scenario.weather` (Acto 1: n01–n04 día despejado, n05 noche). Inyectado vía constructor (`setEnvironment`), retrocompatible.
  75 tests; typecheck/lint/build OK.
- 2026-06-12 (palo dentro / palo fuera): los postes y el travesaño dejan de ser un evento terminal plano y pasan a ser **tubos que rebotan** (`resolvePostBounce` en `src/core/collisions.ts`): segmentos eje (postes verticales + travesaño) con reflexión `v' = v − (1+e)(v·n)n`, `POST_RESTITUTION=0.62`, solo dentro de `POST_Z_BAND=0.4`. `ShotCollider` marca `postHit`: tras pegar, **palo y adentro = GOAL**, **palo y afuera = POST** (antes OUT). `Flight.onBounce`/`hitPost` exponen el rebote → clank en el instante (no doble en el resultado) + zoom; `coach.post.goalPost` para "del palo y adentro". Tests: rebote en poste/travesaño, fuera de banda no rebota, e integración poste→POST. El preview/predicción usan el mismo `ShotCollider`, así que la trayectoria estimada también rebota.
- 2026-06-11 (dificultad — arquero + barrera que salta): **Arquero** (`src/game/goalkeeper.ts` `DEFAULT_GOALKEEPER_CONFIG`): `reactionDelay 0.34→0.3`, `diveSpeed 3.25→5.6`, `standingReachX 0.62→0.85`, `maxReachX 2.05→2.75`, `maxReachY 2.25→2.42`, `saveZ 0.72→0.82`. Cubre el centro y semi-ángulos; los ángulos lejanos (|x|>2.75) siguen siendo gol — la receta canónica al palo lo bate. `loadLevel` clampa dive a [1.6, 8]. **Barrera que salta** (`src/core/collisions.ts`): `BarrierPlayerCollider.riseY` + `raiseBarrier(config, riseY)`; la banda de colisión sube a `[riseY-R, riseY+height+R]` ⇒ el raso pasa por debajo y el techo sube. `WALL_JUMP_RISE=0.55`. Decisión de salto en `Game.launch()` (`Math.random() < wallJumpChance`), solo en el vuelo real (preview/arquero usan la barrera quieta). Actor `BarrierActor.jump()/update()/settle()` anima el salto (peak 0.55 m, 650 ms). `jumpChance` por nivel + `?wallJump=` en sandbox. Tests: raso pasa bajo la barrera saltada, medio se bloquea.
- 2026-06-11 (gráficos — bloom + estadio): post-procesado en `src/render/renderer.ts` (`EffectComposer` + `RenderPass` + `UnrealBloomPass` strength 0.62 / radius 0.5 / threshold 0.82 + `OutputPass`); `Game` renderiza vía `renderFrame` inyectado (default = render directo, retrocompatible para tests). `src/render/stadium.ts`: multitud `InstancedMesh` `MeshBasicMaterial` por color de instancia (3 bloques ~2900, balanceo colectivo en `onBeforeRender`, sin coste por instancia), 4 torres de luz con lámparas emisivas (florecen), pantalla con logo (`CanvasTexture`), vallas detrás del arco. `src/render/world.ts`: cielo en gradiente (`CanvasTexture` de fondo), líneas del campo (área 16.5×40.32, área chica, arco penal r=9.15 fuera del área, punto penal), red más densa (cell 0.3→0.18), fog 55–120. Tribunas/torres fuera del campo (|x|>34). Bundle +28 KB (167 KB gzip). Pendiente validación visual en vivo (no puedo ver el render): ajustar intensidad de bloom, densidad/colores de multitud, posición de pantalla/tribunas.
- 2026-06-11 (loop de juego MVP 1.13–1.16): arquitectura: `main.ts` crea `Game` (corre siempre) + `App` (menú). `App` arranca en el menú; al elegir nivel llama `game.loadLevel(level)` (reconfigura balón, barrera, arquero, ayuda, cámara, pateador in-place — sin recrear objetos de escena). Cada tiro: `onResult`→`recordSessionShot`→`LevelSession.recordShot` actualiza HUD; `advanceAfterResult` decide siguiente intento o panel. El nivel termina al agotar intentos o cuando es imposible alcanzar los goles (`doomed`). Estrellas: 1ª=superar, 2ª/3ª=condición del JSON (perfect_power/target/no_aid_line/kick/all_attempts). El input del tiro está **gated por `inputEnabled`** (false en menú, true con nivel activo) para que clics del menú/botones no se filtren al juego que corre detrás. Arquero visual oculto si `level.keeper==null`. Persistencia en `localStorage` (`dbh:progress:v1`): estrellas por id (nunca baja), monedas al primer clear, desbloqueo en cadena. 22 tests nuevos (level/session/store).
- 2026-06-11 (fix freeze raso/costados + diagnóstico + dialecto): **causa raíz del freeze** = `bounceGround` aplicaba fricción horizontal 0.72 POR CONTACTO; un balón rasante toca el suelo cada frame (micro-botes) ⇒ pierde ~28%/frame ⇒ muere a mitad de cancha sin cruzar `z=0` ni salir del campo ⇒ corría hasta el safeguard de 8 s (el freeze percibido). Arreglo en `src/core/collisions.ts`: `bounceGround` ahora distingue **bote real** (`vy < -0.6`: restitución + fricción de impacto) de **rodadura** (`|vy|` pequeña: pega el balón al suelo y aplica `ROLL_FRICTION_PER_S=0.55` por TIEMPO, no por contacto). Añadida **detección de balón muerto**: en el suelo con velocidad horizontal `< REST_SPEED=0.7` ⇒ `OUT` inmediato. `dt` ahora se pasa a `ShotCollider.update`/`bounceGround` (default `1/120`, retrocompatible). `MAX_FLIGHT_TIME` 8→5 s. Tests nuevos: rodadura conserva velocidad, balón muerto→OUT, y un raso real termina en <2.5 s. **Diagnóstico del coach**: "a las nubes" (`tooHigh`) ahora solo si `maxHeight>2.6`; nuevo `tooHard` para exceso de potencia sin altura (el raso duro sale bajo, no por arriba). 40 tests; typecheck/lint/build OK.
- 2026-06-11 (1.9f — Ejecución FC26 v1): foco en sensación de remate, sin tocar física del solver. Tunables nuevos:
  Carrera (`src/render/actors.ts`): pose de espera 2.8 m atrás + 1.05 m al costado del pie; plantado 0.5 m detrás + 0.34 m al costado; traslación `easeInQuad`; 3 zancadas hasta `RUN_END=0.62`; swing de pierna +0.95→−1.3 en el tramo final (contacto en `p=1`).
  Impacto (`src/game/game.ts`): `STRIKE_MS=200`, `STRIKE_ZOOM_DEG=5` (zoom-in que se abre); `SQUASH_MS=130`, `SQUASH_AMT=0.16`; giro `ROLL_FACTOR=0.22` (rodadura por velocidad) + `SPIN_FACTOR=0.4` (spin de receta).
  Audio (`src/core/audio.ts`): master gain 0.9 + `setMuted`; silbato (trino ~2480 Hz ×2), golpe = ruido HP (cuero) + sine grave 70+20·barras (cuerpo), whoosh bandpass ∝ velocidad, red HP swish, lamento LP descendente, gol = ruido LP con barrido + capa de voces. Silbato solo desde el 2º AIMING (evita crear el AudioContext antes del primer gesto).
  Entrenamiento (`src/game/coach.ts` + `coach.*` en `es.json`): técnica por tipo de tiro en vivo; diagnóstico post-tiro determinista (gol/perfecto/atajada/palo/barrera/altura/poca potencia/mira). Tests en `coach.test.ts`.
  Pendiente de validación en vivo: ritmo de carrera (600 ms), volumen relativo de las voces de audio, y si el silbato por intento cansa en práctica rápida.
- 2026-06-11 (1.10 — barrera estática): la barrera se construye desde `BarrierSetup { players, distance, spacing?, radius?, height? }` con centro sobre la línea balón→arco y jugadores espaciados lateralmente. La colisión `WALL` vive en `ShotCollider`, por lo que `traceTrajectory`, `TrajectoryPreview` y `Flight` usan exactamente la misma detección. Verificación manual en navegador: `http://localhost:5174/?wall=4&debug=1` muestra `wall 4@9.15m`, `wallHeight`, preview cortado en pared y `result WALL` para el tiro natural central. Tests agregados: hit de cápsula y tiro alto que pasa por encima. Tunables: `DEFAULT_BARRIER_SPACING=0.58`, `DEFAULT_BARRIER_RADIUS=0.26`, `DEFAULT_BARRIER_HEIGHT=1.82` en `src/core/collisions.ts`; URL temporal `wall=<1..6>` y `wallDistance=<6..12>` hasta conectar niveles JSON en 1.13.
- 2026-06-11 (1.11 — arquero básico): se reemplazó la atajada instantánea por `GoalkeeperController`, que planifica desde el cruce estimado del solver (`arcCross`, `timeToGoal`, `estimatedResult`). El actor permanece centrado durante AIMING/RUNUP, espera `reactionDelay`, se lanza en `FLIGHT` y solo fuerza `SAVED` si el balón está cerca del arco y la llegada calculada es viable. Tunables en `src/game/goalkeeper.ts`: `reactionDelay=0.34`, `diveSpeed=3.25`, `standingReachX=0.62`, `baseReachY=1.15`, `maxReachX=2.05`, `minReachY=0.16`, `maxReachY=2.25`, `saveZ=0.72`, `saveGrace=0.05`. URL temporal hasta loader JSON: `keeper=0/off`, `keeperDelay=<0.12..0.9>`, `keeperSpeed=<1.6..6.5>`. QA técnica: tests de plan central/ángulo/no-gol/transición de estado. Ajuste anti-freeze: `Flight.step()` ahora termina al primer evento terminal en vez de esperar a que un `OUT` alto caiga detrás del arco; raso usa hit-stop reducido a 25 ms.
- 2026-06-11 (1.9c.5 — checklist medible): (1) cámara: tope duro `CAM_YAW_CAP=10°`/`CAM_PITCH_CAP=4°`, giro real ~1–2° con la geometría del Acto 1 → **✓ nunca >10°**. (2) lag: el puntero apunta por raycast en el mismo frame; la línea se recalcula al cambiar el input → **✓ <50 ms**. (3) centrado del selector: anclado a `ball.getWorldPosition` ⇒ `selErr≈0px` (`?debug=1`) → **✓ ≪5% del radio**. (4) **receta canónica = 10/10**: chanfle interior (Diego zurdo, contacto x=−0.7) apuntando al costado izquierdo (mira ≈(−1.8, 1.5)) + potencia perfecta 2.75 → cruce determinista (−0.50, 1.55); con jitter humano (±0.35 m mira, ±0.12 barras) 10/10 goles. (5) ciclo: auto-reset 1.2 s (no-gol) saltable con toque; gol incluye repetición ≤2 s saltable → **✓ <2 s entre intentos**. (6) "¿quiero tirar el 11º?" = **subjetivo, lo decide el usuario en vivo**. Parámetros de comba en `physics.ts` (`MAGNUS_S=0.00169`, `MAX_CURVE_SPIN=60`) y óptimos por tipo en `shot-solver.ts` — afinado fino en 1.18.
- 2026-06-11 (1.9d — solver/preview compartidos): `FIXED_TIMESTEP=1/120` exportado desde `core/ballistics.ts` y reutilizado por `Flight` + `TrajectoryPreview`. Cada tiro ahora expone `ShotLaunch { velocity, spin, dragCd, magnusScale }`; la preview usa exactamente ese launch sin dispersión. `?debug=1` muestra además `speed`, `vel`, `elev`, `Cd`, `Magnus`, `wallY`, `cross` y `estimatedResult`. Tunables centrales:
  `MAGNUS_S=0.00169`, `POWER_SPEED_MIN=18`, `POWER_SPEED_MAX=38`, `MAX_CURVE_SPIN=60`, `MAX_TOPSPIN=70` en `src/core/physics.ts`.
  `SHOT_RECIPES.natural/inside_curve/outside_curve/topspin/driven_low/knuckle` en `src/game/shot-solver.ts`: `idealPower`, `idealPowerWindow`, `sideSpin`, `topSpin`, `dragCd`, `magnusScale`, `dispersionBase`.
  Dispersión: `POWER_MISS_FULL_BARS=1.5`, `PRE_RELIEF=0.8`, `BASE_ANGLE_SIGMA=0.05`, `SPEED_WOBBLE=0.4`, `SPIN_WOBBLE=18`.
  Cámara: `CAM_BACK=4.6`, `CAM_SIDE=1.25`, `CAM_HEIGHT=1.45`, `CAM_FOLLOW_FACTOR=0.28`, `CAM_TAU=0.22`, `CAM_YAW_CAP=12°`, `CAM_PITCH_CAP=5°` en `src/render/free-kick-camera.ts`.
  Preview: visible con glow/fade en `src/render/trajectory-preview.ts`; usa el mismo `dragCd` y `magnusScale` del launch real.
- 2026-06-11 (1.9e — reset controlado del tiro): lo roto era el modelo de apuntar a una altura exacta en el arco con corrección iterativa: podía producir líneas casi verticales, ángulos altos y balones que caían sin peligro. Se reemplazó por un modelo más estable: dirección = base al arco en XZ + azimut horizontal + elevación de receta clamp. Valores finales de referencia:
    Velocidad por barra (`src/core/physics.ts`): 1.0→18 m/s, 2.0→23 m/s, 3.0→28 m/s, 4.0→33 m/s, 5.0→38 m/s; 2.85 barras ≈ 27.25 m/s.
  Elevaciones (`src/game/shot-solver.ts`): natural 14° clamp 10–18; inside curve 17° clamp 15–19; outside curve 16° clamp 14–19; topspin 21° clamp 18–23; driven low 3.5° clamp 2–5; knuckle 14° clamp 11–17.
  Drag (`src/game/shot-solver.ts`): natural 0.28, inside 0.30, outside 0.31, topspin 0.32, driven low 0.25, knuckle 0.42.
    Magnus (`src/core/physics.ts`): `MAGNUS_S=0.00169`.
  Spin (`src/game/shot-solver.ts`): inside 58 lateral / -8 topSpin; outside 62 / -5; topspin 5 / -70; driven low 0 / 5; natural 0 / 0; knuckle 0 / 0. El vector final sigue la convención `spin = (topSpin, sideSpin, 0)`.
    Cámara mínima jugable (`src/render/free-kick-camera.ts`): `CAM_BACK=6.6`, `CAM_SIDE=1.05`, `CAM_HEIGHT=1.45`, `CAM_LOOK_HEIGHT=1.2`, `CAM_FOLLOW_FACTOR=0.22`, `CAM_TAU=0.18`, `CAM_YAW_CAP=10°`, `CAM_PITCH_CAP=3.5°`.
- 2026-06-11 (ajuste fino visual/feeling tras prueba en vivo): la línea larga que “caía” hasta el piso estaba vendiendo una sensación equivocada aunque el solver ya fuese correcto. `src/render/trajectory-preview.ts` ahora toma `ShotIntent -> ShotLaunch -> traceTrajectory` con el mismo `dragCd`, `magnusScale` y `FIXED_TIMESTEP`, pero renderiza solo el primer tramo útil como tubo corto (`lineFraction` clamp 0.18–0.55, corte al volver cerca de la altura inicial). El selector de contacto (`src/render/contact-selector.ts`) se recentró sobre el balón, se achicó (`SIZE=1.16 * BALL_RADIUS`), perdió desplazamiento artificial hacia cámara, y el punto rojo/cross ahora avanzan en `Z` según una semiesfera visible (`SURFACE_DEPTH=0.7 * BALL_RADIUS`). El raso se recalibró en `src/game/shot-solver.ts`: `launchAngleDeg=1.8`, clamp `0.4–3.2`, `topSpin=-34`, `magnusScale=0.44`. Curva lateral reforzada: inside `sideSpin=86`, `magnusScale=1.34`; outside `sideSpin=88`, `magnusScale=1.28`. QA técnica ejecutada: `npm run typecheck`, `npm run lint`, `npm run test`, `npm run build` OK.
- 2026-06-11 (reset estructural del tiro, segunda pasada): se retiró el parche `lowShotAssist` tanto de `core/ballistics.ts` como del `launch()` real; el raso vuelve a depender del mismo solver que los demás tiros. `src/game/shot-solver.ts` quedó con un único pipeline: `ShotIntent -> effectiveAimTarget -> solveLaunchVectorForIntent -> ShotLaunch -> traceTrajectory`. La preview en `AIMING` ahora ignora el contacto y dibuja una guía neutra usando el mismo solver/física que el tiro real, evitando vender comba antes de elegir dónde pegarle al balón. El raso quedó con receta baja controlada (`launchAngleDeg=-0.55`, clamp `-1.3..0.1`, `topSpin=-30`, `magnusScale=0.46`, `targetBiasY=-0.18`, clamp de objetivo `0.10..0.58`). El debug suma `aimTarget` y `effectiveAimTarget` para comparar intención vs ajuste de receta. Render del balón retocado en `src/render/ball.ts` con paneles clásicos más sobrios y costuras suaves. QA técnica ejecutada: `npm run typecheck`, `npm run lint`, `npm run test`, `npm run build` OK.
- 2026-06-11 (realineamiento estricto al spec `analisis-video-fc26.md`): la fuente de verdad vuelve a ser el análisis del video. `src/render/aim.ts` reactivó la retícula visible como destino real (anillo + cruz) y la preview larga vuelve a apuntar al arco en vez de morir corta. `src/render/trajectory-preview.ts` en `AIMING` usa el mismo solver del tiro real pero con potencia ideal del tipo actual, para parecerse al lenguaje visual FC26; en `CONTACT/POWERING` sigue usando el input real. La cámara conserva seguimiento casi nulo (`CAM_FOLLOW_FACTOR=0.05`, topes ±3°/±2°). Para el problema del raso que “se iba arriba”, se añadió un control físico explícito del rebote de suelo por tiro (`groundBounceScale`): el `driven_low` casi no rebota (`0.08`), mantiene objetivo más bajo (`targetBiasY=-0.28`, clamp `0.10..0.40`) y usa receta más plana (`launchAngleDeg=-1.1`, clamp `-1.3..-0.25`, `topSpin=-40`, `magnusScale=0.34`). QA técnica ejecutada: `npm run typecheck`, `npm run lint`, `npm run test` OK; pendiente validación manual del usuario en `localhost:5174`.
- 2026-06-11 (corrección post-feedback: retícula imán + raso visual): `solveShotIntent()` ahora resuelve dirección/elevación con la potencia ideal de la receta y luego aplica la potencia real solo como velocidad final. Esto evita que la retícula compense automáticamente un tiro flojo/fuerte; test nuevo cubre que una mala potencia ya no llega igual al destino. El raso se verificó en navegador con `?debug=1`: `launchAngleDeg=-1.30`, `velocity.y=-0.52`, `maxHeight=0.11`, `goalCross y=0.11`. En vuelo, `Game.updateFlight()` usa cámara estática para `driven_low` (`FreeKickCameraRig.updateGroundFlightCamera`) para que el balón no aparezca enorme contra fondo negro aunque vaya rasante. `render/ball.ts` se aclaró con material más luminoso y paneles más chicos; `TrajectoryPreview` vuelve a verde FC26 en `AIMING`. QA técnica: 22 tests verdes antes de build final.
- 2026-06-11 (modo de prueba de ejecutantes + arquero): se agregaron actores visuales low-poly en `src/render/actors.ts` para comparar postura de ejecutante zurdo/derecho sin crear contenido meta. `Q` alterna entre Diego zurdo y el perfil de entrenamiento derecho; al cambiar se espeja `contact.x` para conservar interior/exterior equivalente. El ejecutante se coloca a lados opuestos del balón según pie hábil, la cámara se recalcula con el pie actual, y `?debug=1` muestra `kickerFoot`. Se añadió arquero centrado visible, con seguimiento del cruce estimado y atajada básica (`SAVED`) para tiros de gol en zona central alcanzable; las esquinas quedan como objetivo de prueba.
- 2026-06-11 (1.9): solver con apuntado balístico (bisección sobre la elevación). Decisión: el azimut apunta directo a `aim.x` y la comba se añade como desviación intencional sobre esa base → la receta "apuntar al palo + contacto de comba" dobla hacia adentro. Tunables de feeling en `shot-solver.ts` (POWER_OPTIMAL 2.5–3.5, BASE_ANGLE_SIGMA 0.05, PRE_RELIEF 0.8, etc.) y en `physics.ts` (MAGNUS_S, MAX_CURVE_SPIN). **⚠ Superseded parcialmente por 1.9b:** el apuntado a punto con bisección se reemplaza por elevación derivada del contacto; conservar `traceTrajectory` para la línea de proyección.
- 2026-06-11 (1.3): `MAGNUS_S = 0.00169`. Razón: con S=0.00059 la comba a 25 m daba 1.05 m; Magnus es ~lineal en S, escalé ×(3.0/1.05). Test `ballistics.test.ts` verifica 2.5–3.5 m. Eje de spin vertical (0,ω,0) → comba lateral. Pendiente afinar en 1.18 con el tiro real.
