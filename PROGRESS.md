# PROGRESS.md — DEAD BALL HERO

> Claude Code: lee CLAUDE.md y ROADMAP.md antes de tocar código. Implementa la siguiente tarea `[ ]` en orden, márcala `[x]` al terminar con fecha y nota breve. Un commit por tarea. Si una tarea revela trabajo extra, agrégala como subtarea, no lo hagas en silencio.

## Estado actual
**Fase:** 1 — MVP
**Última sesión:** 2026-06-11 — Tareas 1.1→1.9 completadas. Primer tiro jugable.
**Revisión del usuario (2026-06-11):** ⚠️ probó el build y detectó dos problemas de UX confirmados contra el referente real: (1) la doble barra (potencia + timing) confunde y NO existe en la edición 26 — el referente eliminó el timing verde; (2) la "mira del balón" (grilla 3×3 con punto rojo) no se parece al referente. → **Se agrega el bloque 1.9b (REWORK, prioridad máxima)**. CLAUDE.md, LEVELS.md y ROADMAP.md ya fueron actualizados a la mecánica de la edición 26: leerlos de nuevo antes de tocar código.
**Bloqueos:** ninguno
**Siguiente:** 1.9b.1 → completar todo el bloque 1.9b ANTES de 1.10.

---

## FASE 1 — MVP

### Setup
- [x] 1.1 Inicializar proyecto Vite + TypeScript + Three.js. Estructura de carpetas según CLAUDE.md. ESLint básico. `npm run dev` funcionando. _(2026-06-11: Vite 8 + TS 6 estricto + three r184. Carpetas src/{core,game,render,ui,data/{levels,locales}}. Scripts: dev/build/preview/typecheck/lint. ESLint flat config. Alias `@`→src. main.ts es un placeholder (cubo girando) que se reemplaza en 1.2. typecheck+lint+build OK, `npm run dev`→HTTP 200.)_
- [x] 1.1b Sistema i18n mínimo: helper `t(clave)` + `src/data/locales/es.json` con TODOS los textos del juego desde el primer día (español por defecto, estructura lista para `en.json`). Prohibido hardcodear strings en componentes. _(2026-06-11: `core/i18n.ts` con `t(key, params)` (claves con punto + interpolación `{var}`), `detectLocale()` por navegador, fallback a es, clave cruda + warn si falta (regla LEVELS.md). `es.json` cubre app/menu/settings/hud/result/shot/stars/corner/weather/acts/kickers + los 5 niveles del Acto 1. `main.ts` fija el título vía `t('app.title')`. Estructura lista para `en.json` (Fase 4, idealmente import dinámico).)_
- [x] 1.2 Escena base: campo (plano con textura procedural de césped), arco reglamentario 7.32×2.44 m con red (líneas), iluminación tipo estadio nocturno, cielo oscuro. Cámara detrás del punto de tiro. _(2026-06-11: `core/field.ts` (dims+coords compartidas), `render/renderer.ts` (renderer+cámara+resize, sombras PCF, ACES), `render/world.ts` (césped procedural por CanvasTexture con franjas, arco cilíndrico + red de líneas, 4 torres SpotLight, fog nocturna), `render/ball.ts` (icosaedro flat-shaded). Cámara broadcast detrás del balón.)_

### Física
- [x] 1.3 Módulo `core/ballistics.ts`: integrador del balón con gravedad, drag y Magnus según parámetros de CLAUDE.md. Test unitario: con 3 barras y curva máxima, desplazamiento lateral ≈3 m a 25 m. _(2026-06-11: `core/physics.ts` (constantes + `speedForPower`), `core/ballistics.ts` (`stepBall` Euler semi-implícito con scratch vectors + `traceTrajectory` para test y línea de apuntado). Vitest instalado, test verde. **MAGNUS_S calibrado a 0.00169** → comba ≈3 m a 25 m con 3 barras y `MAX_CURVE_SPIN=60`.)_
- [x] 1.4 Colisiones: postes/travesaño (rebote), red (gol + frenado), suelo (rebote amortiguado), fuera (out). Eventos: `GOAL`, `POST`, `SAVED`, `WALL`, `OUT`. _(2026-06-11: `core/collisions.ts`: `resolveGoalLine` (interpola el cruce en z=0 → GOAL/POST/CROSSBAR/OUT), `bounceGround` (restitución 0.45, fricción 0.72, amortigua spin), `isOutOfPlay`, y `ShotCollider` con estado (recuerda pos previa). Enum incluye SAVED/WALL para 1.10/1.11. `collisions.test.ts`: 7 casos verdes.)_

### Input y mecánica de tiro
- [x] 1.5 Máquina de estados del tiro: `AIMING → CONTACT_SELECT → POWERING → TIMING → FLIGHT → RESULT`. _(2026-06-11: `game/shot-machine.ts`. ⚠️ Se modifica en 1.9b.1: desaparece la fase TIMING.)_
- [x] 1.6 Apuntado: retícula movible (mouse/táctil/teclado), línea de trayectoria parcial cuya longitud viene del stat LÍNEA del pateador. _(2026-06-11: `render/aim.ts`, `game/kicker.ts`, `game/flight.ts`, `game/game.ts`, `ui/hud.ts`. ⚠️ Se modifica en 1.9b.2: el apuntado pasa a azimut + línea de proyección, sin retícula en el arco.)_
- [x] 1.7 Grilla de contacto simplificada MVP. _(2026-06-11: `ContactPad` con grilla 3×3 + punto rojo. ⚠️ Se reemplaza en 1.9b.3 por el balón estilo consola con puntero y etiqueta.)_
- [x] 1.8 Barra de potencia de 5 segmentos + ventana de timing verde. _(2026-06-11: `PowerBar` + `TimingBar` + `core/audio.ts`. ⚠️ La `TimingBar` se ELIMINA en 1.9b.1; la `PowerBar` se conserva y gana el feedback de "potencia perfecta" en 1.9b.4.)_
- [x] 1.9 Mapeo del resultado: (apuntado, contacto, potencia, timing) → velocidad inicial + spin. _(2026-06-11: `game/shot-solver.ts` con apuntado balístico por bisección, `contactToSpin`, `dispersionSigma`, ruido gaussiano. ⚠️ Se adapta en 1.9b: el timing sale de la fórmula de dispersión; el apuntado balístico a punto se reemplaza por elevación derivada del contacto+potencia.)_

### ⚑ 1.9b — REWORK UX: paridad con la edición 26 del referente (PRIORIDAD MÁXIMA, antes de 1.10)
> Origen: prueba en vivo del usuario + verificación contra el referente real. La edición 26 NO tiene timing verde (doble toque): es UNA barra de mantener-soltar. Y la selección de contacto es un balón con puntero fino y etiqueta de tipo de golpe, no una grilla con punto rojo. La especificación completa quedó en CLAUDE.md (sección "La mecánica core") — releerla antes de empezar.

- [x] 1.9b.1 **Una sola barra, sin doble toque.** Eliminar `TimingBar` y la fase `TIMING` de `shot-machine.ts` (nuevo flujo: `AIMING → CONTACT_SELECT → POWERING → RUNUP → FLIGHT → RESULT`). Al SOLTAR el botón/click/touch: potencia fijada + arranca `RUNUP` (carrera del pateador, 600 ms para Diego) + golpeo automático al terminar. Durante POWERING la barra se llena en 1150 ms; si llega al tope sin soltar, dispara al máximo (igual que el referente). Actualizar tests de la máquina. _(2026-06-11: FSM reescrita: fuera TIMING/`TimingBar`; añadida `RUNUP` (`setRunupMs`, Diego 600 ms) + auto-disparo al tope. `onPowerReleased` para 1.9b.4. `green` queda true hasta 1.9b.4. game.ts/hud.ts/css limpiados de timing. 5 tests verdes.)_
- [x] 1.9b.2 **Apuntado por azimut con línea de proyección (sin retícula en el arco).** El arrastre horizontal (o ←/→) rota la dirección del tiro alrededor del balón y la cámara orbita acompañando. La elevación YA NO la elige el usuario apuntando a un punto: se deriva de `contacto.Y` + tipo de tiro + potencia (tabla de elevación base por tiro en `shot-solver.ts`). La línea de proyección sale DESDE EL BALÓN, muestra el primer tramo de la trayectoria real (con comba y elevación actuales, recalculada en vivo) y su largo = stat LÍNEA. Eliminar la retícula del arco. El gesto de juego correcto pasa a ser "alinear la línea sobre/al costado de la barrera", como en el referente. **Encuadre de cámara según `docs/ref-contacto-fc26.png`:** cámara baja detrás y levemente al costado del pateador (lado de su pie hábil), pateador en el tercio izquierdo del cuadro, balón en el tercio inferior-central, arco centrado al fondo. Línea de proyección en verde. _(2026-06-11: FSM `aim`→`{azimuth}` (`setAzimuth`/`nudgeAzimuth`, ±0.5 rad). `shot-solver.ts` reescrito: `classifyContact` (zonas+pie), tabla `BASE_ELEVATION`/`elevationFor`, `horizontalAzimuthDir`+`shotDirection` (azimut + ⇒ +X), `buildInitialState` determinista. `aim.ts`: sin retícula, línea dashed verde con trayectoria real truncada por LÍNEA. `game.ts`: input azimut (puntero abs. + ←/→), cámara orbital baja (`updateAimCamera`), proyección en vivo. **Adelanté la dispersión por potencia de 1.9b.4** (`optimalPowerCenter`/`isPerfectPower`/`PERFECT_POWER_HALF`, `dispersionSigma` usa `|power−centroÓptimo|`); falta solo el feedback (flash/audio/vibración) y los tests de 1.9b.4. Campos de timing eliminados de `ShotInput`. 23 tests verdes. ⚠ encuadre/lado de cámara a verificar en vivo (1.9b.6).)_
- [x] 1.9b.3 **Selector de contacto EN EL MUNDO 3D, calcado de la edición 26.** El usuario aportó captura de referencia: guardarla en `docs/ref-contacto-fc26.png` y replicar su composición (solo como referencia interna, jamás incluirla en el juego ni en assets). Eliminar el `ContactPad` del HUD. Implementar en Three.js, anclado al balón: (a) **grilla romboidal verde semitransparente** — plano billboard inclinado ~45° detrás/alrededor del balón, líneas finas 5×5, opacidad ~0.35; (b) **punto rojo** (sprite) que marca el contacto y se mueve con arrastre táctil/mouse o WASD/stick derecho, limitado al rombo; (c) **cruz/cursor pequeña** junto al punto; (d) **etiqueta inferior en mayúsculas** (franja oscura semitransparente, tipografía condensada) con el tipo de golpe según zona. Claves nuevas en `es.json`: `shot.chanfle_interior` ("CHANFLE CON EL INTERIOR DEL PIE"), `shot.chanfle_exterior` ("CHANFLE CON EL EXTERIOR DEL PIE"), `shot.picada` ("PICADA"), `shot.raso` ("RASO"), `shot.normal` ("GOLPE NATURAL"). Zonas: |x|>0.45 → chanfle interior/exterior según signo y pie del pateador; y>0.45 → picada; y<-0.45 → raso; centro → natural. La grilla y el punto solo son visibles en fases AIMING/CONTACT_SELECT/POWERING; desaparecen al disparar. La PowerBar sigue siendo elemento aparte del HUD (abajo), sin tocar. _(2026-06-11: `render/contact-selector.ts` (Three.js anclado al balón: grilla verde + rombo `LineLoop` + punto rojo `CircleGeometry` + cruz blanca, billboard hacia cámara inclinado 45°, contacto clampeado al rombo `|x|+|y|≤1`, `depthTest:false`). `ContactPad` eliminado del HUD; etiqueta `setContactType` (franja inferior mayúsculas) + claves `shot.chanfle_interior/exterior/picada/raso/normal` en es.json. `game.ts`: input de contacto por puntero relativo (movementX/Y) en CONTACT + WASD; etiqueta vía `classifyContact`. Visible en AIMING/CONTACT/POWERING. 23 tests verdes. ⚠ estética del rombo (grilla 5×5 cuadrada + outline diamante) a pulir en vivo 1.9b.6.)_
- [x] 1.9b.4 **"Potencia perfecta" reemplaza al verde.** En `shot-solver.ts`: `dispersionSigma` ya no recibe timing; recibe `|power - centroÓptimoDelTiro|`. Soltar dentro de ±0.15 barras del centro → dispersión 0 + la PowerBar emite un brillo/flash dorado + `playPerfect()` en audio + `navigator.vibrate(40)` si existe. El rango óptimo por tipo de tiro vive en una tabla (curva: centro 2.75; raso: 2.0; etc., según recetas de CLAUDE.md). Actualizar `shot-solver.test.ts`. _(2026-06-11: solver ya migrado en 1.9b.2 (tabla `OPTIMAL_POWER`, `isPerfectPower`, `PERFECT_POWER_HALF=0.15`, tests). Aquí: `audio.playPerfect()` (arpegio), `PowerBar.setOptimal(center,half)` (banda por tipo) + `flashPerfect()` (glow dorado CSS), `game.onPowerReleased` dispara flash+audio+`navigator.vibrate(40)`. 23 tests verdes.)_
- [ ] 1.9b.5 **Stars y locale.** `StarCondition` cambia `green_timing` → `perfect_power` (LEVELS.md ya actualizado, copiar). Actualizar los 5 JSON de niveles y el texto del nivel a1n03 ("La potencia justa") desde LEVELS.md. Migración de saves: si localStorage tiene estrellas con la condición vieja, conservarlas tal cual (no penalizar al jugador).
- [ ] 1.9b.6 **QA del rework con overlay de debug.** Toggle `?debug=1`: muestra azimut, contacto, potencia, sigma y punto de cruce en z=0. Verificar la receta canónica: línea al costado de una barrera imaginaria + contacto lateral + soltar en 2.75 → gol consistente. Anotar parámetros en Notas de QA. **⚑ avisar al usuario para probar en vivo antes de seguir con 1.10.**

### Mundo
- [ ] 1.10 Barrera estática: N cápsulas según JSON del nivel, colisión = `WALL`.
- [ ] 1.11 Arquero básico: reacción tras un delay paramétrico, se lanza hacia la intersección estimada; si llega, `SAVED` con animación simple.
- [ ] 1.12 Cámara de vuelo que sigue el balón + repetición corta del gol desde detrás del arco.

### Contenido y meta
- [ ] 1.13 Formato JSON de nivel + loader con validación, implementando EXACTAMENTE el esquema de **LEVELS.md** (ya con `perfect_power`). Copiar los 5 niveles del Acto 1 y sus textos de locale desde ese archivo.
- [ ] 1.14 Pateador Diego con stats (PRE 95, POT 80, CUR 92, KNU 60, LÍNEA larga) afectando dispersión y línea de proyección.
- [ ] 1.15 HUD completo: intentos, marcador/contexto del nivel, resultado, estrellas obtenidas, botones reintentar/siguiente.
- [ ] 1.16 Menú mínimo: título DEAD BALL HERO, selección de nivel (con candados y estrellas), persistencia en localStorage.
- [ ] 1.17 Audio mínimo: golpe al balón, palo, red, murmullo/explosión de multitud (Web Audio, sintetizado o CC0).
- [ ] 1.18 QA de feeling: calibrar hasta que la receta canónica de curva (CLAUDE.md) dé gol consistente con potencia perfecta y falle de forma justa al pasarse o quedarse corto. Documentar parámetros finales aquí. **⚑ REVISIÓN CON USUARIO** ("se siente como la consola").
- [ ] 1.19 Build de producción + deploy estático (Vercel/Netlify/GitHub Pages). Probar en un teléfono real.

## Backlog inmediato (Fase 2 — no empezar sin cerrar Fase 1)
- [ ] Gamepad API (mando físico en PC, mapeo estilo consola).
- [ ] Sticks virtuales táctiles con zona muerta configurable + vibración.
- [ ] Contacto completo: knuckleball (gesto vertical + flutter), trivela, caída, debajo de barrera.
- [ ] Spin de refuerzo durante la carrera (gesto del stick derecho en RUNUP, ~+20% comba).
- [ ] Barrera que salta + arquero por niveles.
- [ ] Modo Práctica Libre con overlay de inputs ("modo fantasma").

## Decisiones tomadas
- 2026-06-10: Motor Three.js web (no Frostbite/Unity/Godot) por peso y multiplataforma instantánea.
- 2026-06-10: Niveles 100% data-driven en JSON.
- 2026-06-10: Nombres de pila únicamente; cero marcas registradas.
- 2026-06-11: Nombre definitivo del juego: **DEAD BALL HERO**.
- 2026-06-11: Español como idioma base; i18n desde el día 1, inglés en Fase 4 (lanzamiento mundial).
- 2026-06-11: Momentos Legendarios: cada mentor cierra su capítulo recreando su tiro libre histórico (datos fieles, ambientación genérica, firma visual por gesto y colores, jamás nombres reales).
- 2026-06-11: **El referente es la edición 26 (PS5), no las antiguas.** Sin timing verde: una sola barra mantener-soltar; "potencia perfecta" como skill central; apuntado por azimut + línea de proyección; contacto con puntero y etiqueta. Origen: prueba en vivo del usuario + notas oficiales del referente (eliminación del Timed Finishing).

## Notas de QA / feeling
*(Claude Code: anota aquí cada ajuste de física o timing con su razón.)*
- 2026-06-11 (1.9): solver con apuntado balístico (bisección sobre la elevación). Decisión: el azimut apunta directo a `aim.x` y la comba se añade como desviación intencional sobre esa base → la receta "apuntar al palo + contacto de comba" dobla hacia adentro. Tunables de feeling en `shot-solver.ts` (POWER_OPTIMAL 2.5–3.5, BASE_ANGLE_SIGMA 0.05, PRE_RELIEF 0.8, etc.) y en `physics.ts` (MAGNUS_S, MAX_CURVE_SPIN). **⚠ Superseded parcialmente por 1.9b:** el apuntado a punto con bisección se reemplaza por elevación derivada del contacto; conservar `traceTrajectory` para la línea de proyección.
- 2026-06-11 (1.3): `MAGNUS_S = 0.00169`. Razón: con S=0.00059 la comba a 25 m daba 1.05 m; Magnus es ~lineal en S, escalé ×(3.0/1.05). Test `ballistics.test.ts` verifica 2.5–3.5 m. Eje de spin vertical (0,ω,0) → comba lateral. Pendiente afinar en 1.18 con el tiro real.